---
all:
  vars:
    ansible_connection: ssh
    ansible_become_method: sudo

  children:
    outpost_servers:
      hosts:
        192.168.1.191:
        # 192.168.1.131:

    prod:
      children:
        outpost_servers: