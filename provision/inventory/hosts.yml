---
all:
  vars:
    ansible_connection: ssh
    ansible_become_method: sudo

  children:
    ungrouped:
      hosts:
        localhost:

    outpost_servers:
      hosts:
        192.168.1.191:

    prod:
      children:
        outpost_servers:

    test:
      children:
        ungrouped:
