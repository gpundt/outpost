---
- name: Restart NetworkManager
  ansible.builtin.systemd_service:
    name: NetworkManager
    state: restarted

- name: Reload systemd daemon
  ansible.builtin.systemd_service:
    daemon_reload: yes