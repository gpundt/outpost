# Interface to use (auto-populated with Makefile)
interface=wlan0

# Driver to use
driver=nl80211

# AP information
ssid=Outpost
hw_mode=g
channel=1
macaddr_acl=0
auth_algs=1
ignore_broadcast_ssid=0
wpa=2
ieee80211w=0
ieee80211ax=1
wpa_passphrase=password
wpa_key_mgmt=WPA-PSK
wpa_pairwise=CCMP
rsn_pairwise=CCMP